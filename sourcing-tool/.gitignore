# Keep the data directory and all results
# (GitHub Actions commits results here)

# Python
__pycache__/
*.pyc
*.pyo
.env

# Database
data/*.db

# Playwright session (contains auth cookies — never commit)
data/mercari_session.json
